# archive

Das **Lebendige Archiv** selbst: Aphorismen, frei verfasste Schriften und (später)
die Videotranskriptionen — aufbereitet als Jupyter-Book-Inhalt.

Dies wird die zentrale Sammlung, auf der die Startseite aufbaut.
Quellen (Rohdaten) liegen in `../fb_export` und `../transkripte`; hier entsteht
das kuratierte Ergebnis.
